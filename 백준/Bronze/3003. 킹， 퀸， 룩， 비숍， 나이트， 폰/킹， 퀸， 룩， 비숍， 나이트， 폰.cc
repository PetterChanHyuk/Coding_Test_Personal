#include<iostream>
using namespace std;

int main()
{
    int k, q, l, b, kn, p;
    int x;
    cin >> k >> q >> l >> b >> kn >> p;
    x = 1 - k;
    cout << x << " ";
    x = 1 - q;
    cout << x << " ";
    x = 2 - l;
    cout << x << " ";
    x = 2 - b;
    cout << x << " ";
    x = 2 - kn;
    cout << x << " ";
    x = 8 - p;
    cout << x << " ";
    return 0;
}